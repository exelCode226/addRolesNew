import { z } from "zod";

export const createProgramacionSchema = z.object({
    // empleado: z.unknown({
    //     required_error:'Se requiere el empleado'
    // }),
    // pedido: z.unknown({
    //     required_error:'Se requiere el pedido'
    // })
});